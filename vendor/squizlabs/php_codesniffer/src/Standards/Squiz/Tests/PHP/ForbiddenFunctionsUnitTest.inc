<?php
$newfunc = create_function( '$a,$b', 'return "ln($a) + ln($b) = " . log($a * $b);' );
print 'hi';
print( $var );
if ( is_null( $blah ) === true ) {
}

class Test {
	const DELETE = 'delete';
}

?>
